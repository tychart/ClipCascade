Pillow==10.4.0
plyer==2.1.0
pycryptodome==3.20.0
pystray==0.19.5
Requests==2.32.3
websocket_client==1.8.0
xxhash==3.5.0
beautifulsoup4==4.12.3
aiortc==1.10.0
# pystray needs PyGObject for the AppIndicator tray backend. Without it the
# tray falls back to its legacy X11 backend, which has no menu. Building
# PyGObject needs system headers, e.g. on Fedora:
#   sudo dnf install gobject-introspection-devel cairo-gobject-devel
PyGObject>=3.42
